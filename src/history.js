//create history object instead of react router

import {createBrowserHistory} from 'history'; //added auto by react router dom

//creates history object we can maintain instead
export default createBrowserHistory();